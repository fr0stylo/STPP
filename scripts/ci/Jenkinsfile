node {
    ws("${JENKINS_HOME}/jobs/${JOB_NAME}/builds/${BUILD_ID}/build") {
        withEnv(["GOPATH=${JENKINS_HOME}/jobs/${JOB_NAME}/builds/${BUILD_ID}"]) {
            env.PATH="${GOPATH}/bin:$PATH"
    
            stage('Build'){
                sh "go get -v ../cmd/config"
                sh "CGO_ENABLED=0 GOOS=linux go build -a -installsuffix cgo -v ../cmd/config"
            }
        }
    }
}